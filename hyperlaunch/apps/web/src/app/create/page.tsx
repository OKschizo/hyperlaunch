export default function CreatePage() {
  return (
    <div className="page-wrap py-10">
      <h1 className="text-text-hi text-2xl font-semibold">Create Collection</h1>
      <p className="text-text-md mt-3">Wizard coming next steps. Start with Details → Upload → Deploy → Setup.</p>
    </div>
  );
}

